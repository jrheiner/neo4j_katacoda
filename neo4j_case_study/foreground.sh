#/bin/bash

echo 'This is a bash shell script.'
echo 'Any commands you put in here will be executed at runtime.'
echo 'As such, this is a good place to install and configure anything you want to have ready for the learner before they get started.'
echo 'The file is named foreground.sh because when it executes, the commands are echoed to the terminal.'
echo 'To run a script in the background, so learners will not see it execute, specify it in the index.json as "background": "background.sh"'